import { AppRouter } from "./routers/AppRouter"

export const HeroesApp = () => {
    return (
        //colocamos el AppRouter el cual renderiza internamente el HeroesApp
        <AppRouter/>
    )
}
